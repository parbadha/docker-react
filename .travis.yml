sudo: required
services:
  - docker

before_install:
  - docker build -t pardeepb/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pardeepb/docker-react npm run test -- --coverage

language:
  generic
