sudo: required
services:
  - docker

before_install:
  - docker build -t pardeepb/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pardeepb/docker npm run test -- --coverage

language:
  generic
